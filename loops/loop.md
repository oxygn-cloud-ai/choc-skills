Check for available work:

**JQL note:** epic scoping uses `parent = CPT-3` (this is a next-gen Jira project; `"Epic Link" = CPT-3` returns 0).

1. Search Jira CPT-3 for highest-priority bug in Ready for Coding state
2. If found: pick it up per your protocol (plan, TDD, fix, push)
3. If none: report idle status.
