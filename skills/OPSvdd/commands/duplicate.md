# OPSvdd:duplicate — Force Duplicate OPS Ticket (stub, Phase 0)

**Phase 0 — v1.0.0 — not yet implemented.**

Display the following verbatim and stop. Do not proceed to any other action.

```
/OPSvdd duplicate — not yet implemented in v1.0.0.

This subcommand lands in CPT-87.2 alongside OPS publication. It bypasses the same-day
duplicate check that Step 1 performs, letting an operator force a fresh DD cycle even
if another assessment was published for the same vendor slug on the same date.

v1.0.0 ships Phase 0 scaffolding only — installer, router, doctor, version, update, help.

See ~/.claude/skills/OPSvdd/SKILL.md for the phase roadmap, or run:
  /OPSvdd help
```

Exit non-zero (status 2) so automation can detect the stub.
