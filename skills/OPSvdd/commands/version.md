# OPSvdd:version — Show Installed Version

Read the `version:` field from `~/.claude/skills/OPSvdd/SKILL.md` frontmatter and display:

```
OPSvdd v<version>
```

Do not continue after displaying the version.
